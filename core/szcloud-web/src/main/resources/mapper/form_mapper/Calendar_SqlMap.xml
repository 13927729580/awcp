<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.szcloud.framework.formdesigner.core.domain.Calendar">
<!-- <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/> -->
     <!-- <cache type="org.mybatis.caches.memcached.LoggingMemcachedCache" />  -->
	<resultMap id="rm" type="org.szcloud.framework.formdesigner.core.domain.Calendar">
		<id property="id" jdbcType="VARCHAR" column="ID"/>
		<result property="title" jdbcType="VARCHAR" column="TITLE"/>
		<result property="description" jdbcType="VARCHAR" column="DESCRITPTION"/>
		<result property="calendar" jdbcType="VARCHAR" column="CALENDAR"/>
		<result property="allDay" jdbcType="TINYINT" column="ALL_DAY"/>
		<result property="start" jdbcType="TIMESTAMP" column="START"/>
		 <result property="end" jdbcType="TIMESTAMP" column="END"/> 
		<result property="userId" jdbcType="INTEGER" column="USER_ID"/>
	</resultMap>
	<sql id="Base_Column_List">
		ID,TITLE,DESCRIPTION,CALENDAR,ALL_DAY,START,END,USER_ID
	</sql>

	<sql id="Select_Example_Where_Clause">
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid"> 
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value}
									and
									#{criterion.secondValue}
								</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem"
										open="(" close=")" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>

	<!-- 查询条件 -->
	<sql id="EQ_Where_Clause">
		where 1=1
		<trim suffixOverrides=",">
			<if test="id != null and id != ''">
				and ID = #{id}
			</if>
			<if test="title != null and title != ''">
				and TITLE = #{title}
			</if>
			<if test="description != null and description != ''">
				and DESCRIPTION = #{description}
			</if>
			<if test="calendar != null and calendar != ''">
				and CALENDAR = #{calendar}
			</if>
			<if test="allDay != null and allDay != ''">
				and ALL_DAY = #{allDay}
			</if>
			<if test="start != null and start != ''">
				and START = #{start}
			</if>
			<if test="end != null and end != ''">
				and END = #{end}
			</if>
			<if test="userId != null and userId != ''">
				and USER_ID = #{userId}
			</if>
		</trim>
	</sql>
	
	 <!-- 插入 -->
	<insert id="insert" keyProperty="id" flushCache="true" useGeneratedKeys="true">
		INSERT INTO p_fm_calendar (
			ID,
			TITLE,
			DESCRIPTION,
			CALENDAR,
			ALL_DAY,
			START,
			END,
			USER_ID
		) VALUES (
			#{id},
			#{title},
			#{description},
			#{calendar},
			#{allDay},
			#{start},
			#{end},
			#{userId}
		)
	</insert>
	
	<select id="load" parameterType="java.lang.String" resultMap="rm">
		SELECT 
			<include refid="Base_Column_List" />
		FROM p_fm_calendar 
		WHERE 
			ID = #{id} 
	</select>
	<!-- 查询所有的ID -->
	<select id="findAllIds" resultType="java.lang.String">
		SELECT ID FROM p_fm_calendar
	</select>
	<!-- 按Id删除 -->
	<delete id="delete" parameterType="java.lang.String">
		DELETE FROM p_fm_calendar WHERE
		ID = #{id} 
	</delete>
	<!-- 按Id删除 -->
	<delete id="remove" parameterType="java.lang.String">
		DELETE FROM p_fm_calendar
		WHERE
		ID =
		#{id} 
	</delete>	
	
	
	<select id="countAll" resultType="java.lang.Long">
		SELECT count(ID) FROM p_fm_calendar	
	</select>
	
	<select id="get" parameterType="java.lang.String" resultMap="rm">
		SELECT
		<include refid="Base_Column_List" />
		FROM p_fm_calendar
		WHERE
		ID = #{id} 
	</select>
	
	<select id="getAll" resultMap="rm">
		SELECT
		<include refid="Base_Column_List" />
		FROM p_fm_calendar
	</select>
	
	<select id="eqQueryList" resultMap="rm" parameterType="Object">
		select
		<include refid="Base_Column_List" />
		from p_fm_calendar
		<include refid="EQ_Where_Clause" />
	</select>
	<select id="queryCountByExample" resultType="INTEGER" 
		parameterType="org.szcloud.framework.core.domain.BaseExample">
		select count(*) as C from (
		select
		<if test="distinct">
			distinct
		</if>
		<include refid="Base_Column_List" />
		from p_fm_calendar
		<if test="_parameter != null">
			<include refid="Select_Example_Where_Clause" />
		</if>
		) tmpcount
		
	</select>
	<select id="selectByExample" resultMap="rm"
		parameterType="org.szcloud.framework.core.domain.BaseExample">
		select
		<if test="distinct">
			distinct
		</if>
		<include refid="Base_Column_List" />
		from p_fm_calendar
		<if test="_parameter != null">
			<include refid="Select_Example_Where_Clause" />
		</if>
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
		<if test="start !=0 or limit!=0">
			limit #{start},#{limit}
		</if>
		
	</select>
	<!-- 更新 -->
	<update id="updateSelective" parameterType="org.szcloud.framework.formdesigner.core.domain.Calendar">
		UPDATE p_fm_calendar 
			<set>
				<if test="id != null and id != ''">
					and ID = #{id}
				</if>
				<if test="title != null and title != ''">
					and TITLE = #{title}
				</if>
				<if test="description != null and description != ''">
					and DESCRIPTION = #{description}
				</if>
				<if test="calendar != null and calendar != ''">
					and CALENDAR = #{calendar}
				</if>
				<if test="allDay != null and allDay != ''">
					and ALL_DAY = #{allDay}
				</if>
				<if test="start != null and start != ''">
					and START = #{start}
				</if>
				<if test="end != null and end != ''">
					and END = #{end}
				</if>
				<if test="userId != null and userId != ''">
					and USER_ID = #{userId}
				</if>
			</set>
		WHERE 
			ID = #{id}
	</update>
	
	<update id="update" parameterType="org.szcloud.framework.formdesigner.core.domain.Calendar">
		UPDATE p_fm_calendar SET
			TITLE = #{title},
			DESCRIPTION = #{description},
			CALENDAR = #{calendar},
			ALL_DAY = #{allDay},
			START = #{start},
			END = #{end},
			USER_ID = #{userId}
		WHERE 
			ID = #{id} 
	</update>		
	
</mapper>

